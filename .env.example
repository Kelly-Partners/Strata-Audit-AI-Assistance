# Azure Functions base URL (all API endpoints go through this)
# .env.local uses localhost for development; .env.production uses the Azure URL for builds
VITE_AZURE_FUNCTION_URL=http://localhost:7071

# Azure AD Authentication (Microsoft Entra ID)
VITE_AZURE_AD_CLIENT_ID=
VITE_AZURE_AD_TENANT_ID=
